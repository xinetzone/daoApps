# Taolib Secrets（需在部署前手动创建或通过 sealed-secrets 管理）
apiVersion: v1
kind: Secret
metadata:
  name: taolib-secrets
  namespace: taolib
  labels:
    app.kubernetes.io/part-of: taolib
type: Opaque
stringData:
  jwt-secret: "CHANGE_ME_IN_PRODUCTION"
